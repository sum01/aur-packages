# Maintainer: Sum1 https://github.com/sum01
pkgname=('vcash')
pkgver=0.6.0.3
pkgrel=1
pkgdesc="A decentralized currency for the internet."
arch=('x86_64')
url="https://github.com/openvcash/vcash"
license=('AGPL3')
depends=('openssl' 'db' 'boost')
makedepends=('screen' 'curl' 'gcc<=5.3.0-5') # Remove version from GCC when this issue is fixed: https://github.com/openvcash/vcash/issues/14
backup=('~/vcash/backups/' '~/.vcash/backups/')
source=("https://github.com/openvcash/vcash/archive/${pkgver}.tar.gz")
sha256sums=('7b67e98bb250b8e6c03462f31f403a8ffb5a84366c86bf7f05bd0bd3ab03617b') # SKIP is added for vcash-electron because it does not have snapshots yet

package() {
	# This is where CMake would go, if this merge request ever gets commited: https://github.com/openvcash/vcash/pull/4
	# I'll get back to this later, although it might be mostly unnecessary if that pull is ever merged

	# Need to run through the linux install script, and rehash it into something that works here.
}
